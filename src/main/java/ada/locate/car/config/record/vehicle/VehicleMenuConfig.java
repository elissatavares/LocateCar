package ada.locate.car.config.record.vehicle;

//menu que vai acessar os meus controllers
public record VehicleMenuConfig(VehicleControllerConfig controller,
                                ProviderVehicleConfig provider) {
}
