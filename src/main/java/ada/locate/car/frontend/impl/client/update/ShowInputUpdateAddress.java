package ada.locate.car.frontend.impl.client.update;

import ada.locate.car.frontend.api.ShowInputOnlyField;

public class ShowInputUpdateAddress extends ShowInputOnlyField {
    public ShowInputUpdateAddress(String title, String description) {
        super(title, description);
    }
}
